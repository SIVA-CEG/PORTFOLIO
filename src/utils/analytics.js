export function track(evt, props){
// Hook point to fire analytics events. Implement per your chosen analytics provider.
// Example: window.gtag && window.gtag('event', evt, props)
console.log('analytics', evt, props)
}